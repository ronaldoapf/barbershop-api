services:
  db:
    container_name: barbershop
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: barbershop
    ports:
      - "5432:5432"